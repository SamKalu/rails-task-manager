<h1>Task List</h1>

<ul>
  <% @tasks.each do |task| %>
    <%= link_to tasks_path(task.id) do %>
      <li><%= task.title %>: <%= task.details %> - Completed: <%= task.completed %></li>
    <% end %>
  <% end %>
</ul>